package io.github.dalayach.Print_tools;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

 /**
  *  Tests Build_strings functions individually for their individual traits. Each test function may be wildly different
  *  from the rest.
  *  
  */

public class Test_for_Build_strings
{

   private Build_strings bs = new Build_strings();
   private String expected_string;
   private String[] arr = {"ace", "ventura", "show"};
   private int expected_int;


   /**
    * A test for the method print_center_justified()
    */   
   @Test public void test_print_center_justified()
   {
   
   
   
   }   
}